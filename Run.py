import os

import pytest

    # # 执行命令，生成xml文件
    # gengeratexml = "pytest -v -s --alluredir=outputs/report/xml "
    # print("gengeratexml:", gengeratexml)
    # os.system(gengeratexml)
    # # 生成报告，--clean会清除旧文件
    # generatehtml = "allure generate report/xml -o report/html --clean"
    # print("generatehtml:", generatehtml)
    # os.system(generatehtml)

if __name__ == '__main__':
    pass
